name: Browser Build & Web Deploy

on:
  push:
    branches: [ "ci"]

jobs:
    build:
        runs-on: lmql-ci
        steps:
            - name: Install Dependencies
              run: ls && pip install -e .
            - name: Test
              run: bash scripts/wheel.sh $1
